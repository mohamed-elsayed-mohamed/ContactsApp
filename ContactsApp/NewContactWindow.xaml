<Window x:Class="ContactsApp.NewContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContactsApp"
        mc:Ignorable="d"
        Title="NewContactWindow" Height="220" Width="250" MinHeight="220" MinWidth="250" WindowStartupLocation="CenterScreen">
    <StackPanel Margin="10">
        <TextBlock Text="Name"/>
        <TextBox x:Name="txtName" Margin="0 0 0 5"/>
        <TextBlock Text="Email"/>
        <TextBox x:Name="txtEmail" Margin="0 0 0 5"/>
        <TextBlock Text="Phone"/>
        <TextBox x:Name="txtPhone" Margin="0 0 0 5"/>
        <Button x:Name="btnSave" Content="Save" Click="btnSave_Click" Style="{StaticResource NewButton}"/>
    </StackPanel>
</Window>
